export default function decodeGraph(graph: any, query: any, links?: any[]): {};
export function getRangeNodes(
  graph: any,
  {
    key,
    end,
    limit,
  }: {
    key: any;
    end: any;
    limit?: number;
  },
): any[];
