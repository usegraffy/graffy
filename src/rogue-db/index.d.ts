declare function _default({
  collection,
  indexes,
  links,
  pollInterval,
}?: {
  collection: any;
  indexes?: any[];
  links?: any[];
  pollInterval?: number;
}): (store: any) => void;
export default _default;
